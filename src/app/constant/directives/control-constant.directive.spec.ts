import { ControlConstantDirective } from './control-constant.directive';

describe('ControlConstantDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlConstantDirective();
    expect(directive).toBeTruthy();
  });
});
