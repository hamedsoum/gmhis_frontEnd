export enum SHSize {
    EXTRA_LARGE = 'extra-large',
    LARGE = 'large',
    MEDIUM = 'medium',
    SMALL = 'small',
    EXTRA_SMALL = 'extra-small',
}

export type SHSizeStr = 'extra-large' | 'large' | 'medium' | 'small' | 'extra-small';

export enum SHColor {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    LIGHT = 'light',
    DISCRETE = 'discrete'
}

export type SHColorStr = 'primary' | 'secondary' | 'light' | 'discrete';
